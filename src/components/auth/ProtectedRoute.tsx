/**
 * ProtectedRoute
 *
 * Re-exported from @rhizome/core so the shared implementation is used.
 * Import this module within cubefsrs instead of @rhizome/core directly
 * to keep import paths stable if the shared location ever changes.
 */

export { ProtectedRoute } from "@rhizome/core";
